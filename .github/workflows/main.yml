name: practice-ci-cd
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          git clone https://github.com/apix23/github-actions-masterclass.git
          ls -la
      